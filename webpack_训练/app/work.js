/**
 * Created by Administrator on 2017/6/2.
 */
var config=require('./config.json');
document.write(config.text);
const sum = (a,b)=>a+b;
var foo = 'bar';
var baz = {foo};
function f(x, y) {
  return {x, y};
}
alert(sum(1,2))
